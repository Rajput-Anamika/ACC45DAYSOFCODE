#include <iostream>
using namespace std;

int main() {
	int t;
	cin>>t;
	while(t--){
	    float s,a,b,c,per;
	    cin>>s>>a>>b>>c;
	    per=(c*s)/100;
	    s=s+per;
	    if((s>=a) && (s<=b))
	    {
	        cout<<"Yes\n";
	    }
	    else{
	        cout<<"No\n";
	    }
	}
	return 0;
}
